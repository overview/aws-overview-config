description "Create the 'overview' user"
author "Overview Project"

start on starting overview-worker
start on starting overview-documentset-worker

pre-start script
  grep overview /etc/group || groupadd overview -g 998
  grep overview /etc/passwd || useradd overview -c "Overview Servers" -d /home/overview -g 998 --system --shell /bin/false -u 998
  mkdir -p /home/overview
  chown overview:overview /home/overview
end script
