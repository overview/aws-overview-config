sudo reload rsyslog || true

sudo stop overview-worker || true
sudo stop overview-message-broker || true
sudo stop overview-documentset-worker || true

sleep 3

sudo start overview-worker
sudo start overview-message-broker
sudo start overview-documentset-worker || true

